export enum QueueEvent {
  ACTIVE = 'active',
  CLEANED = 'cleaned',
  COMPLETED = 'completed',
  DRAINED = 'drained',
  ERROR = 'error',
  FAILED = 'failed',
  PAUSED = 'paused',
  PROGRESS = 'progress',
  REMOVED = 'removed',
  RESUMED = 'resumed',
  STALLED = 'stalled',
  WAITING = 'waiting',
}
